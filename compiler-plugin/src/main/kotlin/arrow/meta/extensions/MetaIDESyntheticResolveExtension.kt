package arrow.meta.extensions

class MetaIDESyntheticResolveExtension
 /* companion object : SyntheticResolveExtension
}*/
 /*override fun generateSyntheticClasses(
  thisDescriptor: ClassDescriptor,
  name: Name,
  ctx: LazyClassContext,
  declarationProvider: ClassMemberDeclarationProvider,
  result: MutableSet<ClassDescriptor>
) {
  when (val c = thisDescriptor) {
    is KtClass -> if (isHigherKindedType(c)) result.add(provideSyntheticClass(c, declarationProvider, ctx)) //declarationProvider.ownerInfo.correspondingClassOrObject
    else -> super.generateSyntheticClasses(thisDescriptor, name, ctx, declarationProvider, result)
  }
}*/

/*
  override fun generateSyntheticProperties(thisDescriptor: ClassDescriptor, name: Name, bindingContext: BindingContext, fromSupertypes: ArrayList<PropertyDescriptor>, result: MutableSet<PropertyDescriptor>) {
    when (val c = thisDescriptor) {
      is KtClass -> if (isHigherKindedType(c)) result.add(provideSyntheticAlias())
      else -> super.generateSyntheticProperties(thisDescriptor, name, bindingContext, fromSupertypes, result)
    }
*/
